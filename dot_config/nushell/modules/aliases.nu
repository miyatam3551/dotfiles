# ===============================================
# エイリアス設定 / Aliases
# ===============================================

# AI/開発ツール
export alias q = q chat --model claude-4-sonnet
export alias cc = claude

# Python/パッケージ管理
export alias python = python3
export alias uvr = uv run
export alias uvs = uv sync
export alias uva = uv add

# インフラツール
export alias tf = terraform
export alias mp = multipass

# コンテナ/オーケストレーション
export alias d = docker
export alias dc = docker compose
export alias k = kubectl

# ユーティリティ
export alias cl = clear

# Git
export alias gs = git status
export alias ga = git add
export alias gc = git commit
export alias gd = git diff
export alias gac = git add . and git commit
export alias gps = git push
export alias gpl = git pull
export alias gsw = git switch

# ghq (リポジトリ管理)
export alias gr = repos
export alias grr = repo-root
export alias grc = repo-clone
export alias gri = repo-info

# Basic
export alias rr = rm -r

# Kubernetes
export alias k = kubectl

# chezmoi
export alias cm = chezmoi

# mkcd (in functions.nu)
export alias mcd = mkcd
